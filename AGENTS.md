# Project Instructions

這是一個長期維護的網站專案。

## 開發原則
- 不得任意改變既有 UI 配置。
- 優先修改現有檔案，不要在沒有必要時重構整個專案。
- 修改 JavaScript 前，先確認相關功能與相依檔案。
- 不要刪除既有功能，除非使用者明確要求。
- 修改 CSS 時，必須同時考慮桌機版與手機版。
- 優先沿用既有命名、結構與程式風格。
- 若修改可能影響多個頁面，先檢查共用元件與共用樣式。
- 每次修改完成後，清楚列出修改過的檔案與主要變更內容。

## 修改前檢查
開始工作前：
1. 先閱讀本檔案。
2. 查看專案目錄結構。
3. 查看 `git status`。
4. 必要時查看最近的 `git log`。
5. 若有 `PROJECT_CONTEXT.md`、`HANDOFF.md` 或 `TODO.md`，先閱讀後再修改。

## 程式修改原則
- 盡量做最小範圍修改，避免不必要的大規模重寫。
- 不要任意更換框架、函式庫或建置方式。
- 不要新增不必要的相依套件。
- 若發現舊程式有問題，但不在本次工作範圍內，先說明，不要順手大改。
- 修改既有函式時，先確認有哪些地方會呼叫它。
- 若修改資料格式，必須同步檢查所有讀取與寫入該資料的程式。

## UI / UX
- 除非使用者明確要求，不要任意改變：
  - 頁面配置
  - 操作流程
  - 導覽方式
  - 按鈕位置
  - 響應式行為
- 視覺調整應盡量沿用目前網站既有設計語言。
- 修改桌機版時，也要檢查手機版是否受到影響。
- 修改手機版時，不應破壞桌機版。

## JavaScript
- 修改 JavaScript 前，先找出：
  - 事件監聽
  - DOM selector
  - 共用函式
  - 資料來源
  - localStorage / sessionStorage 使用情況
- 不要任意重新命名既有函式或 DOM id/class，除非已確認所有相依處。
- 若新增功能，優先整合進既有架構，而不是建立平行的重複邏輯。

## CSS
- 優先修改既有 class，不要大量加入重複樣式。
- 注意 CSS selector specificity。
- 若專案已有 responsive breakpoint，優先沿用。
- 不要為了解決單一畫面問題而使用大量 `!important`。

## Git
- 修改前先查看 `git status`。
- 必要時查看最近的 `git log` 與相關 commit。
- 不要自行執行 commit、push、pull、merge、rebase 或 reset，除非使用者明確要求。
- 不要自行建立或切換 branch，除非使用者明確要求。
- 若工作目錄已有未提交修改，不要覆蓋或丟棄它們。
- 修改完成後，可建議合適的 commit message，但不要自行提交。

## 跨電腦開發
本專案可能會在多台電腦間交替開發。

每次開始工作時，若存在以下檔案，請先閱讀：
- `PROJECT_CONTEXT.md`：專案架構與重要設計決策
- `HANDOFF.md`：上一個工作環境的交班紀錄
- `TODO.md`：待辦事項

若 Git 記錄顯示其他電腦最近有新修改，先理解最近 commit 的內容，再繼續工作。

## 工作完成後
完成修改後，請回報：
1. 修改了哪些檔案
2. 每個檔案主要修改內容
3. 是否可能影響其他功能
4. 建議使用者測試哪些項目
5. 若適合，提供一個簡潔明確的 Git commit message

## 禁止事項
除非使用者明確要求，否則：
- 不要刪除大量既有程式碼。
- 不要重做整個網站。
- 不要任意更換 UI 架構。
- 不要移除看似未使用但用途尚未確認的檔案。
- 不要覆蓋使用者目前尚未 commit 的修改。
- 不要因為「程式可以更漂亮」就自行進行大規模 refactor。

## 核心原則
先理解現有專案，再修改。
優先維持既有功能與使用者操作方式。
採用最小必要變更，避免非必要重構。
